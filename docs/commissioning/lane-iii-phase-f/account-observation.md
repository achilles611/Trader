# Account observation

One configured master account is selected only by explicit local alias → provider account ID mapping. Account reads normalize active state, balance, realized/unrealized P&L, margin state, and exposed restrictions with source timestamp. Unrelated visible accounts are not selected or passed to strategy code.

Account observation is currently **UNAVAILABLE**. No provider account ID or direct API entitlement has been provided.
