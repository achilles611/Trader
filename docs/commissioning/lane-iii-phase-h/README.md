# Lane III-H sovereign live-capital boundary

L3H is a separate, disarmed capability. It does not alter the frozen Lane III
scientific package or the L3G Sim101 paper capability. A source checkout alone
cannot submit a real-capital order: activation requires a local, signed,
unrevoked capability bound to one exact account, one `MNQ SEP26` contract, one
commissioning epoch, and a fresh clean broker reconciliation.

Current source status is `BLOCKED_CAPABILITY_MISSING`. The repository contains
a dedicated native AddOn source, HMAC/replay-safe gateway, event lifecycle, and
local-only tooling, but no account identity, signing key, credential, compiled
AddOn provenance, or operator activation. The next phase cannot be called
commissioned until an installed Sim101 matrix proves the apparatus. L3H is
mechanical readiness engineering, not evidence of edge or profitability.
