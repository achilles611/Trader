# F5 governance registry

This directory is an offline validation boundary. Its records describe existing authority and dependencies; they never grant trading, wallet, capital, strategy, or scientific authority. Run `python -m src.governance.cli status --root .` for the bounded machine-readable summary.
